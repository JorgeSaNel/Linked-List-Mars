This file create a linked list in Mars language. It has three methods.
	Push method => push(node_t *top, int val): Insert elements on the list until it finds a 0
	Pop method =>  int pop(top){} Remove the first element of the list
	Print method => Print the current list